# --- DeepFleet deepagents worker configuration ---
# Copy this file to ".env" inside the worker/ directory and fill in values.
#   cp env.example.txt .env

# Shared secret. Must match WORKER_TOKEN configured in the DeepFleet web app.
# Leave empty to disable auth (local dev only).
DEEPFLEET_WORKER_TOKEN=change-me-to-a-long-random-string

# Model provider API keys. Provide whichever providers your agents use.
# The model string from DeepFleet looks like "anthropic:claude-sonnet-4-6".
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Comma-separated allowed CORS origins (defaults to *).
DEEPFLEET_CORS_ORIGINS=*

# Set to 1 to allow the run_shell tool to actually execute commands (dangerous).
DEEPFLEET_ALLOW_SHELL=0
